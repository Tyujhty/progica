<div class="flex flex-wrap w-full">

    {% if countShelters is defined %} 
        <div class="w-full h-8 flex items-center pl-4 ">
            <p class=' text-lg'>Nous avons trouvé : {{ countShelters }} {% if countShelters > 0 %} à {% endif %} {{criteriaTown}}</p>
        </div>
    {% endif %}

    <div class="w-full flex flex-wrap">
        {% for shelter in shelters %}
            {% include "_partials/_shelter_card.html.twig" %}
        {% endfor %}
    </div>
</div>