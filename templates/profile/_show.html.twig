{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block body %}
    <div class="flex flex-wrap justify-end w-full lg:w-9/12 max-w-screen-2xl mt-24 lg:mt-32 h-full mx-auto mb-10">
    
        <div class=" lg:fixed lg:bottom-auto lg:left-12 flex flex-col items-center w-full lg:w-3/12 py-4 px-8 lg:border-2 z-20 lg:bg-bgGolorGrayLight lg:rounded-3xl border-bgGolorGrayLight ">
            <div class="w-40 h-40 flex">
                <img src="{{ asset(user.avatar) }}" alt="Avatar de l'utilisateur" class="w-full object-cover border-4 rounded-full border-borderColorLight">
            </div>

            <div class="">
                <h2 class="my-4 font-semibold text-txtBlueDark">Quelques informations sur moi</h2>
                <p><i class="fa-solid fa-location-dot mr-2 text-lg text-txtGray mb-2"></i> {{ user.address }} </p>
                <p><i class="fa-solid fa-envelope mr-2 text-lg text-txtGray mb-2"></i>{{user.email}}</p>
                <p><i class="fa-solid fa-phone mr-2 text-lg text-txtGray mb-2"></i> {{ user.phone }} </p>
            </div>

            <hr class="hidden lg:block w-11/12 my-3">

            <div class="hidden lg:block w-full text-center">
                <h2 class="my-2 font-semibold text-txtBlueDark">Mes gîtes</h2>
                <p>Location disponible : {{ user.shelters|length }} {{ user.shelters|length > 1 ? 'gîtes' : 'gîte' }}</p>
                </p>
            </div>
        </div>        
        
        <div class="w-full px-6 lg:w-9/12">
            <h1 class="text-txtBlueDark text-center lg:text-left text-2xl lg:text-3xl mb-10 mt-8 lg:mt-0">Bonjour, je suis {{ user.firstname }} {{ user.lastname}} </h1>
            <div class="mb-14">
                <h2 class="text-xl font-medium text-txtGray mb-4">A propos</h2>
                <p class='userDescContainer'> {{ user.description }} </p>

            </div>
            <div class="flex flex-col">
                <h2 class="text-xl font-medium text-txtGray mb-4">Gîtes de {{ user.firstname }}</h2>
                <div class="flex flex-wrap justify-around">
                    {% for shelter in user.shelters %}
                        {% include "/profile/_shelterCardProfile.html.twig" %}
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
