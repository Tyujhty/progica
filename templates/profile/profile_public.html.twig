{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex flex-wrap justify-end w-full lg:w-9/12 max-w-screen-2xl mt-24 lg:mt-32 h-full mx-auto mb-10">
    
        <div class=" lg:fixed lg:bottom-auto lg:left-12 flex flex-col items-center w-full lg:w-3/12 py-4 px-8 lg:border-2 z-20 lg:bg-bgGolorGrayLight lg:rounded-3xl border-bgGolorGrayLight ">
            {% include "profile/_profile_sidebar.html.twig" %}
        </div>        

        <div class="w-full px-6 lg:w-9/12">
            <h1 class="text-txtBlueDark text-center lg:text-left text-2xl lg:text-3xl mb-10 mt-8 lg:mt-0">Bonjour, je suis {{ user.fullname}} </h1>
            
            <div class="mb-14">
                <h2 class="text-xl font-medium text-txtGray mb-4">A propos</h2>
                <p class='userDescContainer'> {{ user.description }} </p>
            </div>
            
            <div class="flex flex-col">
                <h2 class="text-xl font-medium text-txtGray mb-4">Gîtes de {{ user.firstname }}</h2>
                <div class="flex flex-wrap justify-around">
                    {% for shelter in user.shelters %}
                        {% include "/profile/_profile_shelter_card.html.twig" %}
                    {% endfor %}
                </div>
            </div>
        
        </div>
    </div>
{% endblock %}
