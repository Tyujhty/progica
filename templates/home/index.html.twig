{% extends 'base.html.twig' %}

{% block title %}Progica - Echappez à la routine, louez un gîte de rêve{% endblock %}

{% block body %}
    <h1>Bienvenue sur la page Home</h1>
    <ul>
        {% for shelter in shelters %}
            <div class="ctn w-4/12 flex flex-rown">
                <a href=" {{path('gite_show', {'id' : shelter.id} )}} ">
                    <h3> {{ shelter.name }} </h3>
                </a>
                <h2> {{ shelter.town }} </h2>
            </div>
        {% endfor %}
    </ul>
{% endblock %}
